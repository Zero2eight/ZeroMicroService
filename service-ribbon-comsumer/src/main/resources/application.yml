server:
  port: 8035
eureka:
  instance:
    hostname: localhost #此处需填写localhost，不然会无法找到主机
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8011/eureka/
spring:
  application:
    name: eureka-ribbon-comsumer
#    name: config-client

